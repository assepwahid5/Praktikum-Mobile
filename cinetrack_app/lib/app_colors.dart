import 'package:flutter/material.dart';

class AppColors {
  // Cinematic Dark Mode Color Palette
  static const Color background = Color(0xFF121826);      // Deep Slate Gray
  static const Color surface = Color(0xFF1E293B);         // Slate Gray (Cards/Dialogs)
  static const Color primary = Color(0xFF00F5D4);         // Neon Teal (Accent/Main buttons)
  static const Color secondary = Color(0xFFF59E0B);       // Gold (Ratings/Highlights)
  static const Color textPrimary = Color(0xFFFFFFFF);     // White
  static const Color textSecondary = Color(0xFF94A3B8);   // Soft Muted Gray
  static const Color danger = Color(0xFFF43F5E);          // Crimson Red (Delete action)
  static const Color success = Color(0xFF10B981);         // Emerald Green (Confirmations)
  static const Color border = Color(0xFF334155);          // Dark Slate Border
}
